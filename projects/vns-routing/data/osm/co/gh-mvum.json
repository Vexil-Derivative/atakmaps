{
  "priority": [
    {"if": "road_environment == FERRY", "multiply_by": 0},
    {"if": "road_class == MOTORWAY", "multiply_by": 1.1},
    {"if": "road_class == TRUNK", "multiply_by": 1.05},
    {"if": "road_class == TRACK", "multiply_by": 0.8},
    {"if": "surface == GRAVEL || surface == UNPAVED || surface == DIRT", "multiply_by": 0.8}
  ],
  "speed": [
    {"if": "road_class == TRACK", "limit_to": 25},
    {"if": "surface == GRAVEL || surface == UNPAVED || surface == DIRT", "limit_to": 25},
    {"if": "max_speed == 0", "limit_to": 15}
  ]
}
